这是一个更倾向于知识库类型的 Wiki 模板。

在应用本主题之后，网站的 Dashboard 内会多出一些配置项。

默认会显示根目录下的文件夹，作为首页的分类列表，也可以自定义分类、以及对应的图标。

除了在 Dashboard 内指定分类的 图标、摘要之外，也可以通过在分类（文件夹）内创建 index.md 的方式，通过其 Metadata 进行声明，图标对应的属性为  `icon` （值应当类似 `fa fa-search` 之类），摘要对应的属性为  `summary`。